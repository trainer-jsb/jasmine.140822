describe('Boolean Check',()=>{
    it('should be truthy',()=>{
        const flag = true;
        expect(flag).toBeTruthy;
    });
    it('should be falsy',()=>{
        const flag = true;
        expect(flag).toBeFalsy;
    });
});